const StatusEventError = require("./status.error");
const DownEventError = require("./down.error");
const UpEventError = require("./up.error");
const InvalidParamError = require("./invalid-param.error");

module.exports = {
    StatusEventError,
    DownEventError,
    UpEventError,
    InvalidParamError
};
